interface:
  display_name: "To iOS Run"
  short_description: "Connect MobileEasyUse and run one iOS operation"
  default_prompt: "Use $to-ios-run to resolve an iOS App, connect through MCP-managed runtime loading, and execute one supplied Inline or Module operation."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "To Android Presets"
  short_description: "Build verified reusable Android App presets"
  default_prompt: "Use $to-android-presets to build and verify reusable presets for this Android App scene."
policy:
  allow_implicit_invocation: false

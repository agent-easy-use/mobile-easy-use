interface:
  display_name: "To Android Presets"
  short_description: "Reusable Android App capabilities for Test and Probe"
  default_prompt: "Use $to-android-presets to generate reusable actions, state access, and scoped overrides for this Android App and verify them when a device is available."
policy:
  allow_implicit_invocation: false

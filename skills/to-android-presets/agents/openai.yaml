interface:
  display_name: "To Android Presets"
  short_description: "Generate reusable Android App probes and declarations"
  default_prompt: "Use $to-android-presets to generate reusable probes for this Android App and verify them when a device is available."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "To iOS Presets"
  short_description: "Reusable iOS App capabilities for Test and Probe"
  default_prompt: "Use $to-ios-presets to generate reusable actions, state access, and scoped overrides for this iOS App and verify them when a device is available."
policy:
  allow_implicit_invocation: false
